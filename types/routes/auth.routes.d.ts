import { Router } from 'express';

/**
 * Router instance for authentication routes:
 * - POST /signup
 * - POST /login
 * - GET /me
 * - GET /logout
 */
declare const router: Router;

export default router;
