// types/routes/rating.routes.d.ts
import { Router } from 'express';

/**
 * Router instance for rating routes:
 * - POST /:movieId  → addRating
 */
declare const router: Router;

export default router;
