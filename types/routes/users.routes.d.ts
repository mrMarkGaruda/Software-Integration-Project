// types/routes/users.routes.d.ts
import { Router } from 'express';

/**
 * Router instance for user routes:
 * - POST /register  → register
 * - POST /login     → login
 */
declare const router: Router;

export default router;
