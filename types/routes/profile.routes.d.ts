// types/routes/profile.routes.d.ts
import { Router } from 'express';

/**
 * Router instance for profile routes:
 * - PUT  /          → editPassword
 * - POST /          → logout
 */
declare const router: Router;

export default router;
